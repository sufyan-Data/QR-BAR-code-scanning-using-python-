# QR-BAR-code-scanning-using-python-
**we are scaning QR/BAR code using python**
```**REQUIRED LIBRARIES**
Required libraries.
-pip install opencv-python.
-pip install numpy.
-pip install pyzbar.
-from pyzbar.pyzbar import decode```
